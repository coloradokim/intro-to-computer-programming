var summerActivities = ['swimming', 'reading at the library', 'tech connect'];

function showActivities(array) {
  for (var i = 0; i < array.length; i++) {
    console.log(array[i]);
  }
};

showActivities(summerActivities)
